pipeline{
	agent any
	stages{
		stage('Init'){
			steps{
				sh 'Hi this is babul'
			}
		}
		stage('Build'){
			steps{
				sh 'Build sample maven project'
			}
		}
		stage('Deploy'){
			steps{
				sh 'Deploy maven project'
			}
		}
	}
}
