pipeline{
	agent any
	stages{
		stage('Init'){
			steps{
				cmd('Hi this is babul')
			}
		}
		stage('Build'){
			steps{
				cmd('Build sample maven project')
			}
		}
		stage('Deploy'){
			steps{
				cmd('Deploy maven project')
			}
		}
	}
}
