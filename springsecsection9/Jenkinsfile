pipeline{
	agent any
	stages{
		stage('Init'){
			steps{
				cmd_exec('Hi this is babul')
			}
		}
		stage('Build'){
			steps{
				cmd_exec('Build sample maven project')
			}
		}
		stage('Deploy'){
			steps{
				cmd_exec('Deploy maven project')
			}
		}
	}
}
